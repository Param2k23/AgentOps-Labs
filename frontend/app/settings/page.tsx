import { PageShell } from '@/components/page-shell'

export default function SettingsPage() {
  return (
    <PageShell
      label="Settings"
      title="Settings"
      description="Placeholder workspace for environment, runtime, and platform preferences."
    />
  )
}
