import { PageShell } from '@/components/page-shell'

export default function LeaderboardPage() {
  return (
    <PageShell
      label="Leaderboard"
      title="Leaderboard"
      description="Placeholder workspace for comparing model performance across benchmark runs."
    />
  )
}
