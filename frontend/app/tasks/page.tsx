import { PageShell } from '@/components/page-shell'

export default function TasksPage() {
  return (
    <PageShell
      label="Tasks"
      title="Tasks"
      description="Placeholder workspace for benchmark tasks and evaluation scenarios."
    />
  )
}
