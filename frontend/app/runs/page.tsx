import { PageShell } from '@/components/page-shell'

export default function RunsPage() {
  return (
    <PageShell
      label="Runs"
      title="Runs"
      description="Placeholder workspace for execution traces, run history, and replayable agent activity."
    />
  )
}
